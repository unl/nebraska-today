{%
  set classes = [
    'node',
    'node--type-' ~ node.bundle|clean_class,
    node.isPromoted() ? 'node--promoted',
    node.isSticky() ? 'node--sticky',
    not node.isPublished() ? 'node--unpublished',
    view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
    'clearfix',
  ]
%}
{{ attach_library('unl_five_news/article') }}

<article{{ attributes.addClass(classes) }}>
{# 
  {{ title_prefix }}
  {% if not page and view_mode != "full" %}
    <h2{{ title_attributes }}>
      <a href="{{ url }}" rel="bookmark">{{ label }}</a>
    </h2>
  {% endif %}
  
  {{ title_suffix }}

  {% if display_submitted %}
    <footer>
      {{ author_picture }}
      <div{{ author_attributes }}>
        {% trans %}Submitted by {{ author_name }} on {{ date }}{% endtrans %}
        {{ metadata }}
      </div>
    </footer>
  {% endif %} #}

  {# <div{{ content_attributes }}> #}
    <div class = "test">
      {# I need to have hover where I hover it will be var(--hover) #}
      <div >
        {{ content.field_article_lead_image }}
      </div>
      <h5 class = "dcf-regular">
      <a href= "{{ url }}"> {{ label }} </a>
      </h5>
      <div style = "font-family: 'Gotham SSm A','Gotham SSm B',Verdana,sans-serif; font-weight: 400; font-style: normal; font-size: .75em;">
       {{ content.field_section }}
      </div>
    </div>

    {# <pre> {{ dd() }}</pre> #}
  {# </div> #}

</article>
