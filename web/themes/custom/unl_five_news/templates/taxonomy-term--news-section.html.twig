<div{{ attributes }}>
  {{ title_prefix }}
  {% if name and not page %}
    <h2><a href="{{ url }}">{{ name }}</a></h2>
  {% endif %}
  {{ title_suffix }}

  <div>
     {{ drupal_view('taxonomy_term', 'block_2', term.id) }}
  </div>
 

  {# <div>
    {{ drupal_view('featured', 'block_1') }}
  </div>
  <div>
    {{ drupal_view('featured', 'block_2') }}
  </div> #}

</div>
